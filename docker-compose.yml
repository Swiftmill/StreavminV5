version: "3.9"
services:
  streavmin:
    image: node:18-alpine
    working_dir: /app
    command: ["node", "server.js"]
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - ./data:/app/data
    environment:
      NODE_ENV: production
    restart: unless-stopped
